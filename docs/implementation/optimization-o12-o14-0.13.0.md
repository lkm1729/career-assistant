# O12–O14：第31–36项 · 0.13.0

日期：2026-09-19。基线：0.12.15。O7–O11已获用户确认完成；本轮31–36已实现并完成本机分层验证。未执行Git提交或推送。

## 已完成

- **O12（31、32、34）**：网页链接与本地文件入口分成独立卡片，加入图标、边框、标题层级、焦点/悬停反馈、窄窗响应和减少动态效果；“补充证据”用户文案统一为“补充材料”；独立评估/匹配历史入口突出标题、数量和展开提示。
- **O13（33）**：支持用户主动拖拽文件/图片，以及文件粘贴事件进入待导入清单；另提供剪贴板图片按钮，读取经Electron剪贴板边界取得的PNG。所有输入先经过名称、扩展名、大小和数量校验，之后仍须逐项确认用途才进入本机解析；不自动勾选或发送，不读取粘贴的纯文字/路径，不让外部拖拽导航页面。传递给主进程的是受限字节，不是文件路径。
- **O14（35）**：数据库自增编号仍是稳定内部身份；界面显示编号改为按当前非回收站记录连续计算。删除10份后新增显示V11，删除全部后新增V1；恢复历史会重新进入显示序列，来源、回滚和回收站身份不混淆。
- **O14（36）**：评估工作台保存独立本地快照。删除评分/匹配历史不会清空当前结果、输入或材料；删除全部、重启、单独清空和撤销均有明确行为，历史列表清空不代表当前工作台清空。

## 验证证据

- 类型检查：通过。
- 聚焦单元/服务测试：20项通过，覆盖字节导入边界、用途与默认选择、评估历史删除/重启快照、连续显示编号/恢复。
- 完整单元/服务测试：**709项通过**，`.test-data/o12-o14-unit-final.log`。
- Electron桌面回归：**18/18通过**，`.test-data/o12-o14-desktop.log`；包含已有O10回归、四页O12/O13交互和O14评估/版本持久化。
- 构建：`npm run build`通过，`.test-data/o12-o14-build.log`。
- Windows打包：`npm run package`通过，产物位于 `release/0.13.0/`；ZIP SHA256 `6AFF886C5E396FA63A75686389A1F5AF2B27E2D0EC61EBB2EBF3C477FE39C728`，安装程序 SHA256 `FBF35A53A3FF9A9F7F6DC9B7DC67153E0712072BF59F91F7C01050C2BC36CF1D`。
- 发布静态校验：通过，`.test-data/o12-o14-verify-release.log`；包含 app-only fuses、ASAR完整性、版本0.13.0、无测试资料入包。
- 最终硬化EXE：隔离启动/正常关闭/重开 **2/2通过**，`.test-data/o12-o14-exe-smoke.log`；未安装到正式目录。构建保留既有大chunk提示、作者元数据和许可文本缺口，不宣称零警告。
- 本轮未调用真实供应商，未在干净机器上验收，未解决既有签名/许可发布阻塞；这些不因本轮功能通过而自动关闭。

## 当前章程进度与剩余工作

| 批次 | 建议 | 状态 | 剩余工作 |
| --- | --- | --- | --- |
| O7–O11 | 21–30 | 用户确认完成 | 无本批开发待办 |
| O12 | 31、32、34 | 0.13.0本机实现、桌面、打包验证完成 | 用户实测视觉与导航确认 |
| O13 | 33 | 0.13.0本机实现、桌面、打包验证完成 | 用户实测真实系统拖拽/剪贴板图片确认 |
| O14 | 35、36 | 0.13.0本机实现、桌面、打包验证完成 | 用户实测历史语义与升级数据确认 |

本机代码和测试完成不等于公开发布闭环：`0.13.0`安装包、签名、许可、干净机、真实供应商仍需按既有发布边界单独确认。
