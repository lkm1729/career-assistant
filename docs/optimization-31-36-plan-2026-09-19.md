# 优化章程 O12–O14：第31–36项 · 0.13.0

日期：2026-09-19。基线：0.12.15。用户已确认O7–O11完成，并授权本轮31–36；不执行Git提交或推送，不触碰正式用户数据库或旧发布产物。

## 进度与剩余工作

| 小节 | 建议 | 状态 | 剩余工作 |
| --- | --- | --- | --- |
| O12 入口与术语 | 31、32、34 | 用户后续确认功能/优化完成 | 无本章程开发待办 |
| O13 文件交互 | 33 | 用户后续确认功能/优化完成 | 无本章程开发待办 |
| O14 历史语义 | 35、36 | 用户后续确认功能/优化完成 | 无本章程开发待办 |

每次进度同步列明当前状态、验证层级和剩余工作。目标统一交付0.13.0，未验证项目不写成完成。

## 验收范围

- O12：网页和文件入口各有边框、显著标题、图标、焦点/悬停反馈；浅色、深色、窄屏和减少动态效果均可用。用途名称统一为“补充材料”，内部evidence值和原有用户保存的提示词不改。评分/匹配历史保留原生键盘可操作展开入口，显著显示标题、数量及展开提示。
- O13：用户主动粘贴或拖拽文件/图片进入本页待导入清单，逐项确认用途后才解析保存；可取消、移出；不截获普通输入框文本粘贴，不读取纯路径文字，不导航外部拖入链接，不自动勾选或发送。继续遵守16项、12MiB/项、格式限制、跨页隔离和运行中锁定。
- O14/35：显示编号按现存正文版本顺序连续计算；剩10份后新增V11，剩0份后新增V1。记录身份不变，回收站不计入正文数量；恢复时按原顺序重新参与编号。回滚、删除确认、父版本、切换前草稿均保留正确身份。当前工作版本仍受保护；要删除全部正文记录，先用既有清空工作台或切换机制解除当前身份。
- O14/36：删除评分/匹配历史（含全部）只移除历史列表条目，当前结果与输入、材料保持不变；当前显示独立保存并跨重启保留，不重新出现在历史列表。单独清空工作台及其撤销仍可用。

## 实施与验证边界

采用ask-matt路由中的明确验收→实现→双轴审查，不初始化tracker、不创建任务、不执行技能内默认提交。测试复用既有MaterialBridge、AiStore/DocumentHistory、WorkbenchStore和Electron界面验收边界；使用隔离数据库、mock供应商，不调用真实供应商。源码修改前快照：`.test-data/o12-o14-source-before/`。

本机单元/服务、开发桌面、最终ASAR、实际EXE及独立干净机分别记录。既有签名、许可和干净机限制不因本轮功能通过而自动关闭。


执行结果：20项聚焦测试、709项全套测试、18项Electron桌面回归及0.13.0构建已通过；详见 `docs/implementation/optimization-o12-o14-0.13.0.md`。


## 2026-09-19 · O12网页入口排版补修 · 0.13.1

用户反馈：31–36功能大致达成，文件入口认可；网页标题过小、被推到右侧，说明文字逼近卡片边界。

- 根因：旧 `.link-drafts summary span` 把新标题当成9px右侧注解；通用 `.materials details` 的较高选择器优先级覆盖了卡片内边距。
- 修正：去掉不再适用的注解样式；网页summary显式分为图标/18px加粗标题/展开标记与下一行副标题；说明文字允许自然换行；卡片采用与文件区一致的内边距。不改网页读取、确认、材料用途及存储逻辑。
- 验证状态：实际桌面复现旧版9px失败，新版四页回归4/4通过；709项单元/服务通过，类型与格式通过。最终ASAR 8/8通过，硬化EXE两轮通过，ZIP218文件逐项一致，静态发布校验通过。
- O12：0.13.1补修及本机交付完成，等待用户视觉确认。O13/O14：本轮不改动，维持0.13.0既有验证状态；不把“功能大致达成”扩写为全部验收关闭。

补修完整记录见 [0.13.1实施记录](implementation/optimization-o12-web-layout-0.13.1.md)。


## 2026-09-19 · O14结果标题编号补修 · 0.13.2

用户进一步发现设计简历/撰写求职信右上角“AI正式结果”仍显示累计编号V19/V8，而当前正文显示V1。已在真实桌面复现，并修正共享标题格式化：正式、历史、手动编辑来源统一调用versionLabel，内部记录身份不变。

验证：709项单元/服务、开发专项2/2、最终ASAR10/10、加强两页旧记录编号复验2/2、实际硬化EXE两轮、静态发布校验和ZIP218文件哈希一致均通过。O12/O13不改动；O14本轮开发与交付待办已完成，剩余用户确认标题显示。不扩大签名/干净机/许可等公开发布声明。

详见[0.13.2补修记录](implementation/optimization-o14-result-label-0.13.2.md)。


## 2026-09-19 · 章程确认与独立BUG

用户随后明确“所有的功能和优化已经做完”。据此将O12–O14验收确认更新为完成。Claude评分JSON语法问题独立跟踪，不重开31–36或扩大本章程范围；0.13.3提供本机取证入口，0.13.4依据用户主动提交的真实响应实施成对正文引号兼容，详见[BUG记录](implementation/claude-score-prose-quotes-0.13.4.md)。真实供应商复测、签名及干净机等限制不视为本章程新增开发任务。
