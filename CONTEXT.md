# Career Assistant

四个独立求职工具的共同领域词汇。用户可以直接选择需要的工具，不要求先设计简历。

## Language

**标签页**：设计简历、简历评分、岗位匹配度评估、撰写求职信中的一个独立工具入口。
_Avoid_: 必经步骤、共享资料工作流

**求职任务**：属于单个标签页的一组输入资料、生成或评估活动及其历史。不同求职任务的材料互相隔离。
_Avoid_: 全局求职档案

**资料项**：用户为当前求职任务提供的文件、图片或网页内容，带有简历、岗位资料或补充材料等用途。

**补充材料**：用于支持用户经验与资格的材料，例如证书、奖项、作品集或项目说明；不是必须执行的指令。

**运行**：一次生成、调整或评估尝试。失败或取消的运行不等同于正式成果。

**正文版本**：设计简历或撰写求职信中一份成功保存的正文成果，具有显示编号和稳定的来源关系。显示编号按现存正文记录连续排列，删除或恢复后会重排；不是记录身份。
_Avoid_: 自动覆盖的最新结果

**当前工作版本**：用户选定作为工作区正文与后续调整基础的版本；手动编辑保留这一来源，查看历史详情本身不改变它。

**回滚版本**：将工作区正文恢复为选定版本并继续使用该记录的当前显示编号，不创建新正文版本，也不删除后续历史。
_Avoid_: 恢复为新版本、删除后续历史

**切换前草稿**：工作区切换或回滚前保留的可恢复内容，包括尚未形成正式版本的手动修改。

**版本回收站**：已从正文版本列表移出的旧版本集合，可按原记录身份恢复，不影响当前工作区。

**评估记录**：一次完成的简历评分或岗位匹配分析，保留当时的评价与依据，不参与正文版本回滚。

**供应商配置**：用户为某个 AI 服务建立的连接设置，可包含多个模型配置。

**模型配置**：供应商配置下的模型 ID、显示名称、能力说明及默认参数。显示名称不作为实际请求中的模型 ID。

**完整评分**：规定的全部维度均已获得所需资料并完成评价的综合分数。

**部分评价**：有维度缺失或不可评价时提供的已完成分项意见，不冒充完整评分。
